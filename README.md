# Docker container for STAR

To use the container simply run :

```
docker run -v "your/data/directory":/data qrouchon/star:2.7.3a STAR [...]
```

For more information on how to use STAR check the documentation.
